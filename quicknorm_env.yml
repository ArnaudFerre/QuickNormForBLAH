name: tf_env  # Nom de l'environnement
channels:
  - defaults
dependencies:
  - python=3.8  # Version de Python souhaitée
  - scipy
  - tensorflow-gpu==2.2.0  # 2.4.1 ? ok mais tf-text plante / 2.0.0 need cuda 10.2
  - matplotlib 
  - pip
  - pip:
    - pronto==2.5.6
    - tensorflow-text==2.2.0  # 2.4.1 ? ok mais tf-text plante
    - nltk
    - tensorflow-hub
