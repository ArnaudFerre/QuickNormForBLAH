name: tf_env  # Nom de l'environnement
channels:
  - defaults
dependencies:
  - python=3.8  # Version de Python souhaitée
  - scipy
  - tensorflow-gpu==2.4.1
  - matplotlib 
  - pip: 
    - tensorflow-hub --upgrade --proxy=http://webproxy.lab-ia.fr:8080
    - pronto==2.5.6 --proxy=http://webproxy.lab-ia.fr:8080
    - tensorflow-text==2.4.1 --upgrade --proxy=http://webproxy.lab-ia.fr:8080
    - --user -U nltk --proxy=http://webproxy.lab-ia.fr:8080
