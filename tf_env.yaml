name: tf_env  # Nom de l'environnement
channels:
  - defaults
dependencies:
  - python=3.10  # Version de Python souhaitée
  - scipy
  - matplotlib 
  - pip
  - pip:
    - tensorflow==2.11.0  # tf-gpu is deprecated
    - tensorflow-text==2.11.0  # 2.2.0/2.4.1 seems bad with tf-text...
    - tensorflow-hub
    - pronto==2.5.6
    - nltk
